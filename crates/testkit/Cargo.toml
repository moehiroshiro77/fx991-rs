[package]
name = "testkit"
description = "Shared test fixtures: locating the ROM and data files"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# None: this crate only locates data files, so it needs nothing but `std`.
